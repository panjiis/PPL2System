name: Build and Push Backend Docker Images

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-gateway:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata (tags, labels) for Docker
        id: meta-go
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/gateway-service
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=sha,format=long

      - name: Build and push Go image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.go  
          push: ${{ github.event_name != 'pull_request' }} 
          tags: ${{ steps.meta-go.outputs.tags }}
          labels: ${{ steps.meta-go.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # build-analytics:
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: read
  #     packages: write

  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4

  #     - name: Set up Docker Buildx
  #       uses: docker/setup-buildx-action@v3

  #     - name: Log in to GitHub Container Registry
  #       uses: docker/login-action@v3
  #       with:
  #         registry: ghcr.io
  #         username: ${{ github.actor }}
  #         password: ${{ secrets.GITHUB_TOKEN }}

  #     - name: Extract metadata (tags, labels) for Docker
  #       id: meta-py
  #       uses: docker/metadata-action@v5
  #       with:
  #         images: ghcr.io/${{ github.repository }}/analytics-service
  #         tags: |
  #           type=ref,event=branch
  #           type=semver,pattern={{version}}
  #           type=sha,format=long

  #     - name: Build and push Python image
  #       uses: docker/build-push-action@v5
  #       with:
  #         context: ./analytical_service
  #         file: ./analytical_service/Dockerfile.py   
  #         push: ${{ github.event_name != 'pull_request' }} 
  #         tags: ${{ steps.meta-py.outputs.tags }}
  #         labels: ${{ steps.meta-py.outputs.labels }}
  #         cache-from: type=gha
  #         cache-to: type=gha,mode=max